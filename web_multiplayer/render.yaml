services:
  - type: web
    name: racinggame-web-multiplayer
    env: python
    plan: free
    buildCommand: pip install -r web_multiplayer/requirements.txt
    startCommand: uvicorn web_multiplayer.server:app --host 0.0.0.0 --port $PORT
